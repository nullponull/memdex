# Core dependencies
sentence-transformers>=2.2.0
numpy>=1.21.0
faiss-cpu>=1.7.0
tqdm>=4.50.0
python-dotenv>=0.19.0

# Optional: PDF support  (pip install memdex[pdf])
# PyPDF2>=3.0.0

# Optional: EPUB support (pip install memdex[epub])
# beautifulsoup4>=4.0.0
# ebooklib>=0.18

# Optional: LLM providers (pip install memdex[llm])
# openai>=1.0.0
# google-generativeai>=0.8.0
# anthropic>=0.52.0

# Optional: MCP server (pip install memdex[mcp])
# mcp[cli]>=1.2.0
